server:
  servlet.context-path: /auth

spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ...
            client-secret: ...
          google:
            client-id: ...
            client-secret: ...

logging:
  level:
    org.springframework: DEBUG